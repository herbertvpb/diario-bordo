import styled from 'styled-components/native';
import { View } from 'react-native'

export const AircraftOption = styled(View)`
    flex-direction: row;
    align-items: center;
    justify-content: space-between;
    height: 48px;
`

